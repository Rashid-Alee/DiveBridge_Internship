export const Practice = ()=>{


    const students = []

    return(

        <>
        <p>{students.length===0 && "student not found"}</p>
        <p>length of students:  {students.length}</p>
        </>
    )
}